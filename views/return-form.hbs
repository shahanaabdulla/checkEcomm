<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Return Form</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            padding: 20px;
        }
        .return-form-container {
            max-width: 600px;
            margin: auto;
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        .return-form-container h2 {
            margin-bottom: 20px;
        }
        .return-form-container textarea {
            width: 100%;
            height: 100px;
            padding: 10px;
            margin-bottom: 20px;
            border: 1px solid #ccc;
            border-radius: 5px;
        }
        .return-form-container button {
            background-color: #ff0000;
            color: #fff;
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px;
        }
        .return-form-container button:hover {
            background-color: #cc0000;
        }
    </style>
</head>
<body>
    <div class="return-form-container">
        <h2>Return Product</h2>
        <form action="/submit-return" method="POST">
            <input type="hidden" name="orderId" value="{{orderId}}">
            <textarea name="returnReason" placeholder="Reason for return" required></textarea>
            <button type="submit">Submit Return Request</button>
        </form>
    </div>
</body>
</html>
